#ifndef ENUM_HPP
#define ENUM_HPP

enum class token_type {
    IF,
    ELSE,
    WHILE,
    FOR,
    RETURN,
    FUNCTION,
    VAR,
    LET,
    CONST,
    TRUE_,
    FALSE_,
    NULL_,
    FLOAT_LITERAL,
    NUMBER,
    PLUS,
    MINUS,
    MULTIPLY,
    DIVIDE,
    LPAREN,
    RPAREN,
    SEMICOLON,
    EQUALS,
    NOT_EQUALS,
    LESS_THAN_OR_EQUAL,
    GREATER_THAN_OR_EQUAL,
    LESS_THAN,
    GREATER_THAN,
    ASSIGN,
    LBRACE,
    RBRACE,
    COMMA,
    IDENTIFIER,
    STRING_LITERAL,
    CHARACTER_LITERAL
};

#endif
